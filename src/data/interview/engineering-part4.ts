import type { InterviewQuestionItem } from "@/lib/types";

// ============================================================
// 七、编程能力与工程实践 Q76-Q85
// ============================================================
export const codingQuestions: InterviewQuestionItem[] = [
  {
    slug: "q76-langchain-components",
    qNumber: 76,
    title: "请你用LangChain实现一个完整的RAG问答系统，包括文档加载、分割、向量化、检索和生成。",
    question: "请你用LangChain实现一个完整的RAG问答系统，包括文档加载、分割、向量化、检索和生成。",
    answer: "```python\nfrom langchain.document_loaders import TextLoader, PyPDFLoader\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\nfrom langchain.embeddings import HuggingFaceEmbeddings\nfrom langchain.vectorstores import Chroma\nfrom langchain.chat_models import ChatOpenAI\nfrom langchain.chains import RetrievalQA\nfrom langchain.prompts import PromptTemplate\n\n# 1. 加载文档\nloader = PyPDFLoader('knowledge.pdf')\ndocuments = loader.load()\n\n# 2. 文档分割\ntext_splitter = RecursiveCharacterTextSplitter(\n    chunk_size=500, chunk_overlap=50,\n    separators=['\\n\\n', '\\n', '。', '！', '？', '；']\n)\ndocs = text_splitter.split_documents(documents)\n\n# 3. 向量化 + 存储\nembeddings = HuggingFaceEmbeddings(\n    model_name='BAAI/bge-large-zh-v1.5',\n    model_kwargs={'device': 'cuda'}\n)\nvectorstore = Chroma.from_documents(\n    docs, embeddings,\n    persist_directory='./chroma_db'\n)\n\n# 4. 自定义Prompt\nprompt_template = '''基于以下上下文回答问题。如果无法回答，直接说不知道。\n{context}\n问题：{question}\n回答：'''\nPROMPT = PromptTemplate(\n    template=prompt_template,\n    input_variables=['context', 'question']\n)\n\n# 5. 构建QA Chain\nqa_chain = RetrievalQA.from_chain_type(\n    llm=ChatOpenAI(model='gpt-4', temperature=0),\n    chain_type='stuff',\n    retriever=vectorstore.as_retriever(search_kwargs={'k': 4}),\n    chain_type_kwargs={'prompt': PROMPT},\n    return_source_documents=True\n)\n\n# 6. 查询\nresult = qa_chain({'query': '什么是RAG？'})\nprint(result['result'])        # 回答\nprint(result['source_documents'])  # 引用来源\n```\n\n**关键设计决策**：\n- 选择适合中文的Embedding模型(BGE)\n- 合理的chunk_size(500)和overlap(50)\n- 自定义Prompt控制行为\n- 返回来源文档实现可溯源",
    category: "编程能力与工程实践",
    tags: ["LangChain", "RAG", "代码实现"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q77-streaming",
    qNumber: 77,
    title: "在大模型应用中，如何实现流式输出 (Streaming / SSE)？请写出关键代码。",
    question: "在大模型应用中，如何实现流式输出 (Streaming / SSE)？请写出关键代码。",
    answer: "**为什么需要流式输出**：\n改善用户体验——首token出现越快，用户等待感越小（TTFT从5s变为0.3s）\n\n**Python FastAPI 实现**（SSE - Server-Sent Events）：\n```python\nfrom fastapi import FastAPI\nfrom fastapi.responses import StreamingResponse\nfrom openai import AsyncOpenAI\nimport asyncio, json\n\napp = FastAPI()\nclient = AsyncOpenAI()\n\nasync def generate_stream(prompt: str):\n    stream = await client.chat.completions.create(\n        model='gpt-4',\n        messages=[{'role': 'user', 'content': prompt}],\n        stream=True\n    )\n    async for chunk in stream:\n        if chunk.choices[0].delta.content:\n            data = json.dumps({\n                'delta': chunk.choices[0].delta.content,\n                'finish_reason': chunk.choices[0].finish_reason\n            })\n            yield f'data: {data}\\n\\n'\n    yield 'data: [DONE]\\n\\n'\n\n@app.post('/chat/stream')\nasync def chat_stream(prompt: str):\n    return StreamingResponse(\n        generate_stream(prompt),\n        media_type='text/event-stream'\n    )\n```\n\n**前端（React/JS）消费SSE**：\n```js\nconst response = await fetch('/chat/stream', { method: 'POST', body: formData });\nconst reader = response.body.getReader();\nconst decoder = new TextDecoder();\nwhile (true) {\n    const { done, value } = await reader.read();\n    if (done) break;\n    const chunk = decoder.decode(value);\n    updateUIRender(chunk);\n}\n```\n\n**WebSocket场景**：适合需要双向通信的实时对话系统",
    category: "编程能力与工程实践",
    tags: ["Streaming", "SSE", "FastAPI"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q78-async-concurrency",
    qNumber: 78,
    title: "当LLM API调用很慢时，如何处理高并发请求？",
    question: "当LLM API调用很慢时，如何处理高并发请求？",
    answer: "**方案**（从易到难）：\n\n**异步IO+协程**：\n```python\nimport asyncio\nfrom openai import AsyncOpenAI\n\nclient = AsyncOpenAI()\nsemaphore = asyncio.Semaphore(10)  # 限制并发\n\nasync def call_llm(prompt: str):\n    async with semaphore:\n        resp = await client.chat.completions.create(\n            model='gpt-4', messages=[{'role':'user','content':prompt}]\n        )\n        return resp.choices[0].message.content\n\nasync def process_batch(prompts: list[str]):\n    tasks = [call_llm(p) for p in prompts]\n    return await asyncio.gather(*tasks)\n```\n\n**消息队列**（异步解耦）：\n请求 -> [API Gateway] -> [Kafka/Redis Queue] -> [Worker Pool] -> LLM API\n-> 结果写入DB/缓存 -> 客户端轮询/WebSocket获取\n\n**本地模型替代API**：\n部署vLLM/TGI自建推理服务 -> 告别API限流 + 更低延迟\n\n**缓存层**：高频相同/相似请求（>90%语义相似度）直接返回缓存结果\n\n**降级策略**：\n- 复杂请求用大模型 -> 超时 -> 降级用小模型\n- 当LLM不可用 -> 返回FAQ匹配结果\n\n**关键权衡**：并发数、延迟、成本的三角平衡",
    category: "编程能力与工程实践",
    tags: ["并发", "异步", "消息队列"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q79-error-handling",
    qNumber: 79,
    title: "如何处理LLM API调用中的各种错误（超时、限流、格式错误等）？给出健壮的重试策略。",
    question: "如何处理LLM API调用中的各种错误（超时、限流、格式错误等）？给出健壮的重试策略。",
    answer: "**常见错误和处理**：\n\n```python\nimport asyncio, time\nfrom openai import AsyncOpenAI, APIError, RateLimitError, APITimeoutError\n\nclient = AsyncOpenAI(max_retries=0)  # 关闭SDK内置重试，用自定义重试\n\nasync def robust_call(prompt: str, max_retries: int = 3):\n    for attempt in range(max_retries):\n        try:\n            resp = await asyncio.wait_for(\n                client.chat.completions.create(\n                    model='gpt-4',\n                    messages=[{'role':'user','content':prompt}],\n                    temperature=0.3,\n                    max_tokens=1000\n                ),\n                timeout=30.0  # 请求超时\n            )\n            return resp.choices[0].message.content\n        except RateLimitError:\n            # 429: 令牌/请求数超限 -> 指数退避+等待\n            wait = 2 ** attempt + random.uniform(0, 1)\n            await asyncio.sleep(wait)\n        except APITimeoutError:\n            # 服务端超时 -> 重试（最多2次）\n            if attempt < max_retries - 1: continue\n            raise\n        except APIError as e:\n            if e.status_code >= 500:  # 5xx服务端错误 -> 重试\n                await asyncio.sleep(2 ** attempt)\n            else:  # 4xx客户端错误 -> 不重试，直接报错\n                raise\n    raise Exception('Max retries exceeded')\n```\n\n**输出解析错误处理**：\n```python\ndef safe_json_parse(response: str):\n    try:\n        return json.loads(response)\n    except json.JSONDecodeError:\n        # 尝试提取markdown代码块中的JSON\n        match = re.search(r'```json\\n(.*?)\\n```', response, re.DOTALL)\n        if match:\n            return json.loads(match.group(1))\n        raise ValueError(f'无法解析JSON: {response}')\n```\n\n**Key Takeaways**：\n- 429/5xx -> 指数退避重试；4xx -> 不重试\n- 输出解析包裹try-catch+后备逻辑\n- 整个函数加超时保护",
    category: "编程能力与工程实践",
    tags: ["错误处理", "重试策略", "鲁棒性"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q80-prompt-management",
    qNumber: 80,
    title: "如何管理大规模Prompt模板？你如何设计Prompt版本管理和A/B测试？",
    question: "如何管理大规模Prompt模板？你如何设计Prompt版本管理和A/B测试？",
    answer: "**Prompt管理**：\n\n- **代码化管理**：像代码(Git)一样管理Prompt（版本控制、Review、测试）\n- **模板引擎**：使用Jinja2/Mustache，Prompt与业务逻辑分离\n- **分层组织**：System Prompt / Task Prompt / Few-shot Examples独立管理层\n\n**版本管理**：\n\n所有Prompt模板存储在Git+DB中（版本号、作者、创建时间、变更记录）\n```yaml\n# prompts/rag_qa.yaml\nversion: 2.3.0\nauthor: zhangsan\nupdated: 2025-01-15\ntemplate: |\n  基于以下上下文回答问题：\n  {context}\n  问题：{question}\n  回答：\n```\n\n**A/B测试设计**：\n\n```python\ndef ab_test_prompt(user_id: str):\n    bucket = hash(user_id) % 100\n    if bucket < 50:\n        return PROMPT_VERSION_A  # 旧Prompt（Control）\n    else:\n        return PROMPT_VERSION_B  # 新Prompt（Treatment）\n```\n\n**指标对比**：用户满意度（点赞/点踩）、回答采纳率、对话轮次、Token消耗\n\n**工具推荐**：LangSmith、PromptLayer、Weights & Biases Prompts",
    category: "编程能力与工程实践",
    tags: ["Prompt管理", "A/B测试", "版本控制"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q81-memory-management",
    qNumber: 81,
    title: "如何实现多轮对话中的上下文窗口管理？处理超长对话的策略有哪些？",
    question: "如何实现多轮对话中的上下文窗口管理？处理超长对话的策略有哪些？",
    answer: "当对话历史超过模型上下文窗口限制（GPT-4=128K, GPT-3.5=16K）时需做窗口管理\n\n**管理策略**：\n\n**滑动窗口**：保留最近N条完整消息\n```python\ndef slide_window(messages, max_messages=20):\n    if len(messages) > max_messages:\n        system_msgs = [m for m in messages if m['role'] == 'system']\n        conversation = messages[len(messages) - max_messages:]\n        return system_msgs + conversation\n    return messages\n```\n\n**滚动摘要**：每N轮对话用LLM生成摘要 + 滑动窗口\n```python\nasync def summarize_history(messages):\n    history_text = format_messages(messages[:10])\n    summary = await call_llm(f'总结以下对话要点：{history_text}')\n    return [{'role': 'system', 'content': f'之前的对话摘要：{summary}'}] + messages[10:]\n```\n\n**Token预算**：动态计算当前用掉的Token，适时裁剪旧的上下文\n```python\nimport tiktoken\nenc = tiktoken.encoding_for_model('gpt-4')\n\ndef trim_to_budget(messages, max_tokens=8000):\n    total = sum(len(enc.encode(m['content'])) for m in messages)\n    while total > max_tokens and len(messages) > 2:\n        removed = messages.pop(1)  # 保留system和最后一个user消息\n        total -= len(enc.encode(removed['content']))\n    return messages\n```\n\n**记忆压缩**：用LoRA训练压缩器模型，将长上下文编码为短向量",
    category: "编程能力与工程实践",
    tags: ["上下文管理", "滑动窗口", "Token预算"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q82-authentication-authorization",
    qNumber: 82,
    title: "大模型应用中的用户认证和权限控制如何设计？API Key安全如何保证？",
    question: "大模型应用中的用户认证和权限控制如何设计？API Key安全如何保证？",
    answer: "**用户认证**：\n- JWT（常用）+ OAuth 2.0（第三方登录）+ SSO（企业）\n- 短期Token（1小时过期）+ Refresh Token续期\n\n**权限控制**（RBAC + ABAC）：\n\n| 角色 | 权限 |\n|------|------|\n| 基础用户 | 每日100次调用，基础模型 |\n| 高级用户 | 每日500次，全模型 |\n| 开发者 | API访问，自定义配置 |\n| 管理员 | 全部权限+管理面板 |\n\n**API Key安全**：\n\n- Key生成：crypto.randomUUID() 或加密哈希\n- 加密存储：SHA256 Hash+Salt存储，不可逆存储\n- 传输安全：HTTPS传输，禁止key出现在URL中\n- Rate Limiting：每个Key独立限流（token-bucket算法）\n- Key轮换：周期性轮换+废弃通知\n\n**架构示例**：\n```python\nfrom fastapi import Depends, HTTPException\nfrom fastapi.security import HTTPBearer\n\nsecurity = HTTPBearer()\n\nasync def verify_api_key(credentials = Depends(security)):\n    key_hash = hashlib.sha256(credentials.credentials.encode()).hexdigest()\n    key_info = await db.get_api_key(key_hash)\n    if not key_info or key_info.expired:\n        raise HTTPException(status_code=403, detail='Invalid API Key')\n    if key_info.rate_limited():\n        raise HTTPException(status_code=429, detail='Rate limit exceeded')\n    return key_info\n```",
    category: "编程能力与工程实践",
    tags: ["认证", "权限控制", "API Key"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q83-logging-audit",
    qNumber: 83,
    title: "如何做好大模型应用的日志记录和链路追踪？",
    question: "如何做好大模型应用的日志记录和链路追踪？",
    answer: "**三层日志体系**：\n\n**应用日志**（结构化JSON）：\n```python\nimport structlog\nlogger = structlog.get_logger()\n\nlogger.info('llm_call',\n    request_id='req_123',\n    user_id='user_456',\n    model='gpt-4',\n    prompt_tokens=250,\n    completion_tokens=180,\n    total_tokens=430,\n    latency_ms=1234,\n    cost=0.0035,\n)\n```\n\n**LLM调用日志**（关键字段）：\n- 请求ID（贯穿全链路）\n- 用户ID\n- 模型名称、参数（temperature/max_tokens）\n- Token数（prompt/completion/total）\n- 延迟（TTFT/total_time）\n- 成本（按token计费）\n- 截断的Prompt和Response（采样存储）\n\n**链路追踪**：\n- OpenTelemetry + Jaeger/Zipkin\n- 每条请求打上trace_id -> 追踪在API Gateway、推理引擎、工具调用之间的完整路径\n- 关键Span：User Request -> Auth -> API Gateway -> LLM Service -> Tool -> LLM -> Response\n\n**存储方案**：\n- 实时日志 -> ElasticSearch + Kibana\n- 长期存档 -> S3/对象存储（成本低）\n- 聚合指标 -> Prometheus + Grafana",
    category: "编程能力与工程实践",
    tags: ["日志", "链路追踪", "可观测性"],
    difficulty: "medium",
    dimension: "engineering",
  },
  {
    slug: "q84-vector-search-scale",
    qNumber: 84,
    title: "千万级向量搜索如何设计架构以支持低延迟和高吞吐？",
    question: "千万级向量搜索如何设计架构以支持低延迟和高吞吐？",
    answer: "**架构设计**：\n\n**索引优化**：\n- 使用近似最近邻索引(ANN)代替精确搜索(KNN)、牺牲少量精度换数量级速度提升\n- 算法选择：HNSW（高精度，内存大）/ IVF-PQ（精度-内存平衡）/ DiskANN（SSD存储）\n- 索引参数：efConstruction（HNSW构建参数）、nlist（IVF聚类数）\n\n**分片 (Sharding)**：\n- 水平切分：数据按ID哈希分片到多个节点\n- 查询扇出(Scatter-Gather)：请求广播到所有分片 -> 各分片独立搜索 -> 汇总归并Top-K\n- 一致性哈希：节点增减影响最小\n\n**副本 (Replication)**：\n- 每个分片N个副本（支持读写分离）\n- 多副本搜索：用最快的副本结果（Tail Latency优化）\n\n**缓存加速**：\n- 高频Query结果缓存（Redis）\n- 向量中心点的「种子搜索」预计算\n\n**基础设施选择**：\n- 100万以下 -> Qdrant/Pinecone单机（最简单）\n- 100万-1亿 -> Milvus集群（分布式）\n- 1亿+ -> Milvus + 自建分布式搜索层\n\n**延迟目标**：单次向量检索 < 10ms（ANN + 内存索引 + 无磁盘IO）",
    category: "编程能力与工程实践",
    tags: ["向量搜索", "架构", "大规模"],
    difficulty: "hard",
    dimension: "engineering",
  },
  {
    slug: "q85-system-architecture",
    qNumber: 85,
    title: "设计一个面向企业的大模型应用平台（如内部知识库问答系统），画出架构图并解释各模块。",
    question: "设计一个面向企业的大模型应用平台（如内部知识库问答系统），画出架构图并解释各模块。",
    answer: "**分层架构**（五层）：\n\n**接入层**：\n- 企业IM（钉钉/企微/飞书）Bot + Web界面\n- 鉴权 + 限流（按部门、角色控制）\n\n**应用层**：\n- 意图识别：用户Query -> 意图分类（FAQ/文档检索/数据分析/工单操作）\n- 对话管理：多轮对话 + 上下文窗口管理 + 记忆\n- 工作流引擎：复杂问题多步处理\n\n**AI能力层**：\n- RAG引擎：Embedding + 向量检索 + Reranker + LLM生成\n- Agent引擎：工具调用 + 规划 + 执行\n- 模型路由：按问题复杂度智能选择模型（小模型 vs 大模型）\n\n**数据层**：\n- 文档库：S3/MinIO存储原始文档\n- 向量库：Milvus集群存储文档Embedding索引\n- 业务数据库：MySQL/PostgreSQL存储用户/权限/对话记录\n- 缓存：Redis存储高频Query答案\n\n**基础设施层**：\n- LLM推理服务：vLLM集群（自建）+ API备份\n- 监控告警：Prometheus + Grafana + 日志平台/ELK\n- CICD：GitOps + Jenkins/GitLab CI\n\n**核心设计决策**：\n- FAQ优先检索 -> 命中直接返回（最快的路径）\n- RAG兜底 -> 未命中FAQ时才触发检索和生成\n- 所有回答引用来源文档（可溯源审计）\n- 人工兜底机制（低置信度问题时转人工客服）",
    category: "编程能力与工程实践",
    tags: ["系统设计", "架构", "平台"],
    difficulty: "hard",
    dimension: "engineering",
  },
];